import React from 'react'

const ChildReducer = (props) => {
    return <div>
        <h4>ChildReducer Component</h4>
        <p>{props.location}</p>
    </div>
}   

export default ChildReducer;